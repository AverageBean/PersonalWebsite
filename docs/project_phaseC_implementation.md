# Phase C Implementation — Concrete Next Steps

**Last updated:** 2026-04-29
**Status:** **Phase C-1 (elliptic cylinders) shipped.** Synthetic test
`EllipticCylinder.stl` now produces volume ratio 0.9999 (was 1.4728 with the
pre-C-1 box fallback). C-2 and C-3 remain.

---

## What's done

- `tools/test-parametric-step.py` now has a `CurvedMinimalPost-Onshape.stl`
  entry locking in the **pre-Phase-C** state as a regression floor:
  - 6 PLANE entities, 0 cylinders/tori/spheres
  - 45.7 % direct quadric coverage; 73.9 % after box-fillet face claiming
  - Volume ratio 1.473 (the box envelopes the curved-out regions)
  - Mean deviation 0.000 mm, Hausdorff 0.001 mm (sample-density artefact —
    the comparison's STL→STEP nearest-point check finds the box surface
    everywhere along the projected ray, since the box contains the post)
- Phase C work must EITHER preserve those bounds (vol 0.9–1.6) OR improve them
  (vol → 1.0 ± 0.05, dev → 0).

## Phase C-1 — DONE (2026-04-29)

Implemented in `tools/convert-stl-to-step-parametric-with-freecad.py`:

- `fit_ellipse_2d(points)` — direct algebraic ellipse fit (Fitzgibbon, Pilu,
  Fisher 1999) via constrained generalized eigenvalue problem.
- `ellipse_distances(points, fit)` — radial-scaling perpendicular-distance
  approximation for inlier classification.
- `detect_elliptic_cylinders(face_centers, face_normals, used, total,
  mesh_triangles)` — RANSAC-style iterative detection on lateral
  (|n·Z| < 0.30) unclaimed faces. Validates axis ratio b/a < 0.95 to avoid
  reclaiming circular cylinders. Z extents come from inlier triangle
  vertices (not centroids — centroid Z range is a tighter inner bound).
- `build_elliptic_cylinder_solid(elliptic_cyls, height_extension=0.0)` —
  CSG path: builds `Part.Ellipse` curves, wires them, extrudes along the
  detected axis. Native STEP export emits `SURFACE_OF_LINEAR_EXTRUSION` on
  an `ELLIPSE` curve.
- Routing in `build_parametric_solid` — elliptic-cylinder path takes
  precedence over the cylindrical/sphere paths (Phase A may have
  spuriously fitted circular cylinders to subsets of an elliptic wall).
- Synthetic test STL `TestDocs/EllipticCylinder.stl` (a=10, b=6, h=30)
  generated by `tools/generate-elliptic-cylinder-test.py`.
- Regression entry in `tools/test-parametric-step.py` — vol ratio
  0.98–1.02, max mean dev 0.05 mm, requires `ELLIPSE` and
  `SURFACE_OF_LINEAR_EXTRUSION` STEP entities.

Two implementation gotchas captured here so the C-2 work doesn't re-walk
them:

1. **Fitzgibbon eigenvalue indexing.** `np.linalg.eig` returns eigenvectors
   as columns: the constraint check `4AC - B² > 0` must use
   `eigvec[0, :] * eigvec[2, :] - eigvec[1, :]**2`, not the row form. Got
   silent "fit degenerate" failures with the row form because none of the
   rows passed.
2. **Canonical-form sign.** The semi-axis formulas have a sign flip vs the
   raw `B² − 4AC` expression: `a² = -num / (denom · ((A+C) + s))`. Without
   the leading `-`, both `a²` and `b²` come out negative for valid ellipse
   fits and the function returns None.

---

## Next step — Phase C-2 (surfaces of revolution)

C-2 (surfaces of revolution) requires:
- Finding a candidate axis (longest axis of unclaimed face cluster, or
  axis of symmetry from PCA on face normals).
- Projecting face centres into (axial_distance, radial_distance) 2D space.
- Fitting a B-spline curve to the (z, r(z)) profile, or detecting it as a
  named primitive (sphere/torus already covered earlier; ogive/parabola for
  C-2 proper).
- Constructing `Part.makeRevolution(spline_face, axis)`.

Reuse from C-1: the lateral-face filter, axis-aligned projection, and
inlier classification machinery all generalise. The new piece is the 2D
profile fit (1D B-spline through (z, r) points) and the revolution
constructor.

The validation strategy mirrors C-1 — generate a synthetic STL via
FreeCAD scripting (e.g. an ogive) since no existing test part has a clean
revolution feature.

## Why C-3 stays deferred

The spec lists C-3 (B-spline fallback) as the catchall for arbitrary curved
surfaces. Two reasons not to jump there next:

1. **C-3 needs B-spline surface fitting.** FreeCAD's Part.BSplineSurface
   can be constructed from a control-point grid, but generating that grid
   from arbitrary face centres requires either (a) parameterising the surface
   onto a 2D domain (hard for general topology) or (b) using a library like
   `geomdl`. Both are a real day or two of work.
2. **C-3 trim curves are non-trivial.** A B-spline patch needs to be trimmed
   to the actual silhouette of the unclaimed face cluster. `BRepAlgoAPI_Section`
   handles this but produces complex topology that can fail boolean ops.

C-2 should land first — same projection machinery as C-1 plus a known
1D-fit problem (revolved profile). C-3 is the bigger bet.

---

## Risk register

| Risk | Likelihood | Mitigation |
|------|------------|------------|
| C-1 false-positive on torus inner-circle faces | Medium | Run AFTER torus detection; check axis-ratio > 0.95 → reject as circle |
| C-1 false-positive on sphere caps | Low | Sphere detection runs first; spherical faces won't be in `unclaimed` |
| Synthetic test STL doesn't trigger the actual code path | Medium | Verify with geometry comparison — vol ratio close to 1.0 |
| FreeCAD's `Part.Ellipse` orientation conventions | Low | Test with rotated ellipses; document the `.LocalAxis` setup |

---

## Phase C scope going forward

- **Session 1 (DONE 2026-04-29)**: detection + CSG for C-1 (elliptic
  cylinders), synthetic test STL, regression entry. Vol ratio 0.9999 on
  the synthetic test. 6/6 parametric tests pass.
- **Next session**: C-2 (surfaces of revolution) — synthetic ogive test
  STL, profile-fit on 2D (z, r) points, `Part.makeRevolution` CSG.
- **Future**: C-3 (B-spline freeform) — multi-day arc on its own.
